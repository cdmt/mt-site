query Fonts {
    viewer {
        fontCollections(first: 20) {
            edges {
                node {
                id
                name
                collectionType
                url
                isNew
                opticalAdjustment
                slug { name }
                }
            }
        }
    }
} 